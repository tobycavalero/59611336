specVersion: "v1beta"
serviceId: "inscribe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inscribe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
